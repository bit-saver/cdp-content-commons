import React from 'react';
import HelpText from '../HelpText';

const PostEmbedHelpText = props => (
  <HelpText>
    <p>I { 'don\'t' } see what { 'I\'m' } looking for..</p>
    <p>What is an Embed Code and why would I use it?</p>
    <p>NEED HELP CONTENT</p>
  </HelpText>
);

export default PostEmbedHelpText;
