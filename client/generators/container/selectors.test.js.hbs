// import { select{{ properCase name }} } from '../selectors';

describe( 'select{{ properCase name }}', () => {
  it( 'Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  } );
} );
